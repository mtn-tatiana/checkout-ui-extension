query RunInput {
  cart {
    buyerIdentity {
      customer {
        email
        numberOfOrders
      }
    }
    lines {
      id
      quantity
      cost {
        totalAmount {
          amount
        }
      }
    }
  }
}
